package main

// In this program we will discuss about basic data types in go

import "fmt"

func main(){
	//there are many types in go they are
	/*
	->int
		-int8
		-int16
		-int32
		-int64
		-int (default 32bit)
	->uint
		-uint8
		-uint16
		-uint32
		-uint64
		-uint (default 32bit)
	->float
		-float32
		-float64
	->bool
		-imp thing to note that bool cannot be converted to integer they are different in go.
	->complex
		-complex
		-complex-64
		-complex128
	*/
	fmt.Println("Variables")
}